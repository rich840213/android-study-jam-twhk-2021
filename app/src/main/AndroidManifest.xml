<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jk.test.androidstudyjamtwhk2021">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidStudyJamTwhk2021">
        <activity android:name=".UnscrambleActivity" />
        <activity android:name=".NavigationActivity" />
        <activity android:name=".AffirmationsActivity" />
        <activity android:name=".TipTimeActivity" />
        <activity android:name=".DiceRollerActivity" />
        <activity android:name=".HappyBirthdayActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>